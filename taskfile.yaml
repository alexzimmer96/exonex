version: '3'

tasks:
  gen-proto:
    cmds:
      - rm -rf ./pkg/api
      - go tool buf dep update
      - go tool buf generate
      - git add .
  add-migration:
    cmds:
      - GOOSE_MIGRATION_DIR="./database/migrations" go tool goose create {{.CLI_ARGS}}